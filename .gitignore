# ============================================
# 白名单模式：仓库只上传 Core 和 Mycode 两部分
# ============================================

# 1. 先忽略根目录下所有内容
/*

# 2. 放行本文件自身
!/.gitignore

# 3. 放行 Core 目录及其全部内容
!/Core/
!/Core/**

# 4. 放行 Mycode 目录及其全部内容
!/Mycode/
!/Mycode/**

# 5. 放行 CubeMX 工程文件 (.ioc)
!/*.ioc

