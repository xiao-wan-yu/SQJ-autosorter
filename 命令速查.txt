=========================================================
  自动分拣机器人 · 命令速查卡（复制改日期/名字就能用）
  两个文件夹：A=工作台(开发+给合作者)  B=保险柜(全量备份+版本)
=========================================================

【① 开工前 · 拉合作者最新代码】
cd G:\STM32\7_AutomatedSortingRobot_26
git pull

【② 开发完 · 保存给合作者（只传源码）】
git add -A
git commit -m "2026.8.27 超声波避障"
git push

【③ 开发完 · 全量备份 + 存一个"当天版本"（一条命令）】
.\同步到全量备份.ps1 -Name "超声波避障"
  (自动: 整个工作区镜像到 SQJ_collab → 提交 → 打版本标签
   backup-2026.8.27-超声波避障 → 推送到你的备份仓库)

【④ 随时回到某天的版本（在 B 里看）】
cd G:\STM32\SQJ_collab
git tag                                  → 列出所有版本
git checkout backup-2026.8.27-超声波避障   → 回到那天(完整快照)
git checkout main                        → 回到最新

【⑤ 临时想看看某天的源码（在 A 里看）】
git checkout v2026.8.15-full-driver      → 回到某标签看代码
git checkout main                        → 看完回到最新
git show mywork:Mycode/car.c             → 找回旧代码里的文件

---------------------------------------------------------
  第一次（仅一次）
---------------------------------------------------------
· 备份仓库 main 第一次推送（备份脚本第一次会失败时用）：
  cd G:\STM32\SQJ_collab
  git push -f origin main --tags

---------------------------------------------------------
  急救（出错时用，不丢代码）
---------------------------------------------------------
git status                        → 看当前状态
git merge --abort                 → 合并卡住时取消
git rebase --abort                → 变基卡住时取消
git diff                          → 看改了哪些内容
git pull 报冲突 → 把报错发给我，不要自己乱删

---------------------------------------------------------
  重要说明
---------------------------------------------------------
· 你的烧录/构建设置(build.bat、.vscode、烧录cfg等)只在本地
  生效，git 已忽略它们，绝不会上传给合作者（skip-worktree）
· 绝不 git push --force 到合作者的仓库
· 开工前必先 git pull，免得覆盖合作者的新代码
